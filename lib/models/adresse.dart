class Adresse{
  String placeFormattedAddress;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  Adresse({required this.placeFormattedAddress, required this.placeId, required this.placeName, required this.longitude, required this.latitude});
}